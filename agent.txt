========================================
 ESTRUTURA DE DIRETÓRIOS (tree)
========================================

.
├── agent.txt
├── app
│   ├── agent.py
│   ├── contract_loader.py
│   ├── main.py
│   └── utils.py
├── prompts
│   └── system_juridico.txt
├── requirements.txt
├── templates
│   ├── index.html
│   └── resultado.html
└── uploads

5 directories, 9 files

========================================
 CONTEÚDO DOS ARQUIVOS
========================================


----------------------------------------
 FILE: ./requirements.txt
----------------------------------------
| fastapi
| uvicorn
| jinja2
| python-docx
| langchain-ollama
| langchain-core
| google-generativeai
| python-multipart
----------------------------------------
 FILE: ./prompts/system_juridico.txt
----------------------------------------
| Você é um Especialista em Redação Contratual da Sanar Contábil S/C Ltda.
| 
| Tarefa:
| Analise o contrato abaixo e gere um relatório técnico seguindo rigorosamente esta estrutura:
| 
| 1. RISCOS JURÍDICOS
| - Liste até 10 riscos de forma objetiva.
| 
| 2. ERROS FORMAIS
| - Se não houver erros: Escreva apenas "Nenhum erro formal encontrado."
| - Se houver: Escreva "Abaixo seguem as correções sugeridas:" e liste os erros de ortografia ou concordância.
| 
| 3. CLÁUSULAS REVISADAS (PRONTAS PARA USO)
| - Identifique as cláusulas críticas ou ambíguas.
| - Redija a nova versão da cláusula de forma técnica e segura.
| - O texto deve estar PRONTO para ser copiado e colado no contrato original.
| - Use este formato:
|     **Cláusula [Nome/Número] - Versão Sugerida:**
|     "[Texto da nova cláusula aqui]"
| 
| Regras:
| - NÃO explique conceitos.
| - NÃO use preâmbulos como "Aqui está sua análise".
| - Vá direto aos pontos.
| - Mantenha o tom formal da Sanar Contábil.
| 
| Contrato para análise:
| {texto_do_contrato}
----------------------------------------
 FILE: ./templates/resultado.html
----------------------------------------

----------------------------------------
 FILE: ./templates/index.html
----------------------------------------
| <!DOCTYPE html>
| <html lang="pt-br">
| 
| <head>
|     <meta charset="UTF-8">
|     <meta name="viewport" content="width=device-width, initial-scale=1.0">
|     <title>Auxiliar de Correção para Contratos | Sanar Contábil</title>
| 
|     <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
|     <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
| 
|     <style>
|         :root {
|             --primary-color: #1a73e8;
|             --secondary-color: #2c3e50;
|             --success-color: #28a745;
|             --bg-color: #f0f2f5;
|             --card-bg: #ffffff;
|             --text-main: #333333;
|         }
| 
|         body {
|             font-family: 'Inter', sans-serif;
|             background-color: var(--bg-color);
|             margin: 0;
|             padding: 40px 20px;
|         }
| 
|         .container {
|             max-width: 1000px;
|             margin: 0 auto;
|         }
| 
|         .header {
|             text-align: center;
|             margin-bottom: 40px;
|         }
| 
|         .header h1 {
|             color: var(--secondary-color);
|             font-size: 2.2rem;
|             margin-bottom: 5px;
|         }
| 
|         .main-card {
|             background: var(--card-bg);
|             padding: 40px;
|             border-radius: 12px;
|             box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
|         }
| 
|         .upload-section {
|             border: 2px dashed #cbd5e0;
|             background: #f8fafc;
|             padding: 30px;
|             border-radius: 10px;
|             text-align: center;
|             margin-bottom: 30px;
|         }
| 
|         .actions {
|             display: grid;
|             grid-template-columns: 1fr 1fr;
|             gap: 20px;
|         }
| 
|         .btn {
|             padding: 18px;
|             border: none;
|             border-radius: 8px;
|             font-size: 1.1rem;
|             font-weight: 600;
|             cursor: pointer;
|             transition: all 0.2s;
|             color: white;
|             text-align: center;
|         }
| 
|         .btn-local {
|             background-color: var(--secondary-color);
|         }
| 
|         .btn-gemini {
|             background-color: var(--primary-color);
|         }
| 
|         .btn-copy {
|             background-color: var(--success-color);
|             padding: 10px 20px;
|             font-size: 0.9rem;
|             margin-bottom: 15px;
|         }
| 
|         .btn:hover {
|             opacity: 0.9;
|             transform: translateY(-1px);
|         }
| 
|         .results-area {
|             margin-top: 40px;
|             border-top: 2px solid #edf2f7;
|             padding-top: 30px;
|         }
| 
|         .results-header {
|             display: flex;
|             justify-content: space-between;
|             align-items: flex-end;
|             margin-bottom: 10px;
|         }
| 
|         #resultado-content {
|             background: #fdfdfd;
|             padding: 30px;
|             border: 1px solid #e2e8f0;
|             border-radius: 8px;
|             font-size: 1.15rem;
|             color: #2d3748;
|             line-height: 1.8;
|         }
| 
|         /* Estilização do texto copiado via MarkDown */
|         #resultado-content strong {
|             color: #1a202c;
|         }
| 
|         #resultado-content blockquote {
|             border-left: 4px solid var(--primary-color);
|             margin: 0;
|             padding-left: 20px;
|             color: #4a5568;
|             font-style: italic;
|         }
| 
|         #loader {
|             display: none;
|             text-align: center;
|             margin: 20px 0;
|             color: var(--primary-color);
|             font-weight: bold;
|         }
| 
|         .footer {
|             text-align: center;
|             margin-top: 50px;
|             color: #888;
|             font-size: 0.9rem;
|         }
|     </style>
| </head>
| 
| <body>
| 
|     <div class="container">
|         <div class="header">
|             <h1>Auxiliar de Correção para Contratos</h1>
|             <p>Sanar Contábil S/C Ltda</p>
|         </div>
| 
|         <div class="main-card">
|             <form action="/analisar" method="post" enctype="multipart/form-data" onsubmit="showLoading()">
|                 <div class="upload-section">
|                     <strong>Selecione o arquivo do contrato (.docx)</strong><br><br>
|                     <input type="file" name="file" accept=".doc,.docx" required>
|                 </div>
| 
|                 <div class="actions">
|                     <button type="submit" name="engine" value="local" class="btn btn-local">Análise Local
|                         (Ollama)</button>
|                     <button type="submit" name="engine" value="gemini" class="btn btn-gemini">Análise Cloud
|                         (Gemini)</button>
|                 </div>
|             </form>
| 
|             <div id="loader">⌛ Processando análise e redigindo cláusulas...</div>
| 
|             {% if resultado %}
|             <div class="results-area">
|                 <div class="results-header">
|                     <h2>Relatório Técnico</h2>
|                     <button class="btn btn-copy" onclick="copyToClipboard()">📋 Copiar Resultado</button>
|                 </div>
| 
|                 <div id="resultado-content"></div>
|                 <script id="raw-data" type="text/plain">{{ resultado | safe }}</script>
|             </div>
|             {% endif %}
|         </div>
| 
|         <div class="footer">
|             &copy; 2024 Sanar Contábil S/C Ltda - Inteligência Documental
|         </div>
|     </div>
| 
|     <script>
|         function showLoading() {
|             document.getElementById('loader').style.display = 'block';
|         }
| 
|         // Renderiza o Markdown
|         window.onload = function () {
|             const rawData = document.getElementById('raw-data');
|             if (rawData) {
|                 const content = rawData.textContent;
|                 document.getElementById('resultado-content').innerHTML = marked.parse(content);
|             }
|         };
| 
|         // Função para copiar o texto
|         async function copyToClipboard() {
|             const rawText = document.getElementById('raw-data').textContent;
|             try {
|                 await navigator.clipboard.writeText(rawText);
|                 const btn = document.querySelector('.btn-copy');
|                 const originalText = btn.innerText;
|                 btn.innerText = "✅ Copiado!";
|                 btn.style.backgroundColor = "#155724";
| 
|                 setTimeout(() => {
|                     btn.innerText = originalText;
|                     btn.style.backgroundColor = "";
|                 }, 2000);
|             } catch (err) {
|                 alert('Erro ao copiar: ', err);
|             }
|         }
|     </script>
| 
| </body>
| 
| </html>
----------------------------------------
 FILE: ./app/main.py
----------------------------------------
| import os
| import time
| from fastapi import FastAPI, UploadFile, File, Request, Form
| from fastapi.responses import HTMLResponse
| from fastapi.templating import Jinja2Templates
| from app.utils import extract_text_from_doc
| from app.agent import AgenteJuridico
| 
| app = FastAPI()
| templates = Jinja2Templates(directory="templates")
| 
| with open("prompts/system_juridico.txt", encoding="utf-8") as f:
|     SYSTEM_PROMPT = f.read()
| 
| agente = AgenteJuridico(SYSTEM_PROMPT)
| 
| @app.get("/", response_class=HTMLResponse)
| async def index(request: Request):
|     return templates.TemplateResponse("index.html", {"request": request})
| 
| @app.post("/analisar", response_class=HTMLResponse)
| async def analisar(request: Request, file: UploadFile = File(...), engine: str = Form(...)):
|     inicio = time.time()
|     
|     conteudo = extract_text_from_doc(file)
|     
|     if engine == "gemini":
|         resultado = agente.analisar_gemini(conteudo)
|     else:
|         resultado = agente.analisar_local(conteudo)
| 
|     tempo_total = round(time.time() - inicio, 2)
| 
|     return templates.TemplateResponse(
|         "index.html",
|         {
|             "request": request,
|             "resultado": resultado,
|             "tempo": tempo_total,
|             "engine_usada": engine.upper()
|         }
|     )
----------------------------------------
 FILE: ./app/utils.py
----------------------------------------
| import os
| import subprocess
| from docx import Document
| 
| 
| def extract_text_from_doc(file):
|     filename = file.filename.lower()
|     temp_path = f"/tmp/{file.filename}"
| 
|     with open(temp_path, "wb") as f:
|         f.write(file.file.read())
| 
|     try:
|         if filename.endswith(".docx"):
|             doc = Document(temp_path)
|             texto = "\n".join(p.text for p in doc.paragraphs)
|             return texto.strip()
| 
|         if filename.endswith(".doc"):
|             result = subprocess.run(
|                 ["antiword", temp_path],
|                 stdout=subprocess.PIPE,
|                 stderr=subprocess.PIPE,
|                 text=True
|             )
| 
|             if result.returncode != 0:
|                 raise RuntimeError(result.stderr)
| 
|             return result.stdout.strip()
| 
|         raise ValueError("Formato de arquivo não suportado")
| 
|     finally:
|         if os.path.exists(temp_path):
|             os.remove(temp_path)

----------------------------------------
 FILE: ./app/contract_loader.py
----------------------------------------
| import subprocess
| from docx import Document
| 
| def load_word(path: str) -> str:
|     if path.lower().endswith(".docx"):
|         doc = Document(path)
|         return "\n".join(p.text for p in doc.paragraphs)
| 
|     if path.lower().endswith(".doc"):
|         result = subprocess.run(
|             ["antiword", path],
|             stdout=subprocess.PIPE,
|             stderr=subprocess.PIPE,
|             text=True
|         )
|         return result.stdout
| 
|     raise ValueError("Formato não suportado")

----------------------------------------
 FILE: ./app/agent.py
----------------------------------------
| import google.generativeai as genai
| from langchain_ollama import ChatOllama
| from langchain_core.messages import SystemMessage
| 
| class AgenteJuridico:
|     def __init__(self, system_prompt: str):
|         self.system_prompt = system_prompt
|         # Configuração Local (Ollama)
|         self.llm_local = ChatOllama(
|             model="phi3:mini",
|             temperature=0.1,
|             num_ctx=4096, 
|             # Adicione ou ajuste este parâmetro se o modelo suportar:
|             num_predict=1024, # Garante espaço para a redação das cláusulas
|             timeout=180
|         )
|         # Configuração Gemini (Cloud)
|         # Substitua pela sua chave ou configure como variável de ambiente
|         genai.configure(api_key="AIzaSyAjKdh8hxRov5_FsAyOLwrcLs9GZfIwPUQ")
|         self.model_gemini = genai.GenerativeModel('gemini-2.5-flash')
| 
|     def analisar_local(self, texto: str) -> str:
|         prompt_final = self.system_prompt.format(texto_do_contrato=texto[:12000])
|         try:
|             resposta = self.llm_local.invoke([SystemMessage(content=prompt_final)])
|             return resposta.content.strip()
|         except Exception as e:
|             return f"❌ Erro Local: {str(e)}"
| 
|     def analisar_gemini(self, texto: str) -> str:
|         prompt_final = self.system_prompt.format(texto_do_contrato=texto)
|         try:
|             # O Gemini 1.5 aceita contextos muito maiores, não precisamos cortar tanto
|             response = self.model_gemini.generate_content(prompt_final)
|             return response.text
|         except Exception as e:
|             return f"❌ Erro Gemini: {str(e)}"